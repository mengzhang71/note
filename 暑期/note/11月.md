您好！我是24届应届生，长沙学院（统招一本）-计算机科学与技术专业，有意应聘前端开发工程师岗位。
个人优势：
本人有6个月前端实习经历，实习期间参与其中政府项目中的公文、事务文件模块，农村智慧管理服务一体化平台、门户网站、数字化大屏等PC/移动端系统的迭代与开发，熟练E-charts完成可视化显示需求等

熟练前端常用技术（HTML5、ES6、TS等），熟悉Vue技术栈（vue-router、axios、vuex等）、uniapp、小程序，熟练element-UI与vant等常用js库，了解http协议、浏览器缓存；熟悉企业前端代码规范、git管理、理解组件化、webpack打包；了解java、SSM框架

在校期间担任计数院实验室助理，加入院内技术类实验室，作为项目组长进行开发，技术一专多长，有多段项目经验，项目内负责过前后端、深度学习，

获得中国软件杯 国三、大学生计算机设计大赛 省三、蓝桥杯web组 省三等学科竞赛奖项
详情可查看我的附件简历，静候您的回复。



# TzList

Condition的options是从这里去获取接口获取过来的

this.choose()





options不好写死，我应该加在哪里好一点



# 复盘

自我介绍



讲一讲你项目里的难点吧



做项目的时候有没有遇到过什么困难，你是怎么解决的



## 八股复盘

说说你对http的理解



讲讲常见的请求



讲讲状态码



讲讲cookie和session



跨域



display布局



js判断数据类型的方法



原型链

作用域链



讲讲防抖和节流



事件循环





# 八股复盘2

讲讲你项目的大屏显示

内存泄漏的情况



你刚刚提到手动移除定时器，为什么要这么做呢不是都已经销毁了吗，

spa页面的路由切换



val let const

我在声明之前使用了变量，会怎么样

、异常什么会导致程序中断



vue生命周期



父子组件的执行顺序，销毁过程呢



wtach与computed

哪个有缓存



手写下事件代理

父有id选择器与五个button子元素，如何选择到每个button节点的文本值



手写下防抖



apply、call、bind





# 八股复盘3

拖拽的插件怎么用，怎么用原生实现



上划刷新下滑加载的使用细节，原生实现方法





为什么不用localStronge而用cookie





如何用定位去滑动时覆盖元素



es5的继承和es6的继承的区别



原型





7天免登陆





e-charts的api











# e-charts业务组件

我在开发中，完成e-charts图表时碰到过各种原因导致的页面未渲染的问题，

首先排查是否是代码有逻辑问题，配置项未配置好导致没渲染，

然后

习惯不好，没有赋长宽，宽高全靠继承，设计稿改动后，一改样式后就出问题，养成给e-charts设宽高的习惯



在开发页面一个组件比较多的页面时e-charts需要的数据是从其他组件传过了的，

还有一些情况，我在写一个能够展开收起的侧面栏里写了e-charts，展开收起的逻辑我首先

格外要注意这些东西



合理运用nextTIck等

一开始规模比较小，没考虑啥东西

在遇到了许多问题、项目里面有一个大屏显示的子系统，做维护和编写的过程

遇到了很多问题，

将e-charts的配置项直接写在页面里的，



导致代码很长，重复度很高，改一点东西就要重写一个option

很难维护，业务层、数据层的东西没有分开，可能在配置项里或者相关方法里操作数据，旁人维护、迁移困难

​	组件化、利用组件通信



考虑到的东西不够，随着开发的页面更加复杂了，渲染不出来，

功能不够完善：我监听了数据，当数据变化能够自动重新刷新图表，添加原生监听器，监听页面变化大小，并在销毁钩子函数里，手动移除原生监听器，防止内存泄漏问题等



```js
  window.onresize = () => pieChart.resize()
首页用的这个形式，好像有时候没有生效

还是换成了这个，好像没问题了
 window.addEventListener('resize', () => {
      // this.chart.resize()
      pieChart.resize()
    })

```

第二行代码使用了 `addEventListener` 方法来添加一个事件监听器，监听 `resize` 事件。通过这种方式，可以添加多个不同的事件监听器，并且不会覆盖之前已经存在的事件处理程序。此外，使用 `addEventListener` 的方式也更加符合现代 JavaScript 的标准，并且在兼容性方面更好。





### weboffice原生http方法

office.WebOpen(formData)

​		SetRequestHeader('Authorization'、.AddForm('signature'

​		这里面调用	this.LOADFILE(httpclient)		----异步请求回来数据，看看服务器有没有返回文件

​				里面调用  httpclient.Open(this.HttpMethod.Post, this.WebUrl, false)

httpclient是http对象			

​	最后httpclient.Clear()

服务器返回 一个文件，并保存到本地做临时文件（存储地址、保存的名字就是之前传入的fileName、Path啥的）

然后打开这个本地文件		this.OpenLocalFile(this.FilePath + this.FileName)		 return this.obj.Open(filePath)





1. **open()**：用于初始化一个请求，指定要发送的请求类型、请求的URL地址以及是否使用异步请求等参数。
2. **send()**：用于将请求发送到服务器。



自己碰到了405状态码，后端没有兼容方法

```js
  this.LOADFILE = function(httpclient) {
    httpclient.ShowProgressUI = true
    if (httpclient.Open(this.HttpMethod.Post, this.WebUrl, false)) {
      if (httpclient.Send()) {
        //判断服务器是否存在文件
        if (httpclient.GetResponseHeader('MsgError') == '404') {
          this.CreateFile()
          //打开文档后，判断当前office版本
          this.getOfficeVersion()
          httpclient.Clear()
          return true
        }
        httpclient.Clear()
        if (this.hiddenSaveLocal(httpclient, this, false, false)) {
          if (this.OpenLocalFile(this.FilePath + this.FileName) == 0) {
            window._local_file = this.FilePath + this.FileName
            this.getOfficeVersion()
            return true
          } else {
            // var windows = window.confirm(
            //     '打开文档错误，可能当前编辑软件不支持当前格式！\r\r单击“确定”关闭。单击“取消”继续。'
            // )
            // if (windows == 1) {
            //     window.close()
            // }
            // window.alert('打开文档错误，可能当前编辑软件不支持当前格式！')
            return false
          }
        }
      }
      return false
    }
  }

```





# 积累

![image-20231219181425456](11%E6%9C%88.assets/image-20231219181425456.png)

这样的图片用定位写在最外层





地图重影问题是因为visualMap中出现没有数据的数据





从用组件，到自己手搓。拖拽的bug优化（中间点击会消失，等荣哥解决）

签名





性能优化可以想个切入点

比如，直报的bug：APP从工作台进个人中心，查看个人头像时，刷新机制不合理，过程太长，能看到默认头像







项目里的聊天室，

聊天的切换，点开不同人的消息的获取，对不同类型消息的处理（比如图片）



```
十七:
那个文档共享的很牛逼，当然难度也很大

十七:
适当看看 找一些实现思路就行

十七:
真正实现是很复杂的

十七:
为的就是要面试的时候有东西说，让面试官觉得你有一定的知识拓展

```





[大专前端，三轮面试，终与阿里无缘 - 掘金 (juejin.cn)](https://juejin.cn/post/7239715208792342584?searchId=202311210930436367A58DD809443FEB1F)



腾讯文档



vue代码按需引入（项目里面有DList，没有看到它引入，但是能直接用，是不是就是这个道理，去问问代哥）

[让你的Vue代码 “学会” 自动按需引入 - 掘金 (juejin.cn)](https://juejin.cn/post/7062648728405934087)



前端变更主题

![image-20231121094722959](11%E6%9C%88.assets/image-20231121094722959.png)



性能优化

https://juejin.cn/post/7263283712225574969?searchId=20231121092530FFE70C84BC81B25F99C6

遇到了xx问题，因为xx(加载图片，接口过多。。。)导致了xx(白屏过长，加载失败，无响应。。。)的结果，采用了xx（代码分割
，骨架屏，懒加载和按需加载。。。）技术等解决，其中我认为xx是核心两点，因为他具备xx的特点，是当时最佳解决方案

往性能优化上引，这不三两句话就能把我给问穿了





就扯e-charts业务组件吧，怎么封装

响应式（跟随页面变化）

碰到很多不显示的问题，



# 月报

9-10

完成：
农业农村移动端-工作台-任务模块->任务日志和任务统计切图
农业农村-行政-事务管理->事务办理的打印页面，迁移word格式打印
进行中：
农业农村的生产、服务、监管、治理四大模块中的智分析更新页面、接口联调等



10-11

完成：

1.农业农村移动端-> 迁移crowd至子系统acceptance，完成三农网课培训、农业经营主体监管、高标准农田实施过程切图与接口联调

2.农口直报系统检查 -> 四大模块的的e-charts编写与细节修复、接口联调、组件封装实现拖拽；各大模块的总览页面的切图与对接台账接口

3.农业农村 - 生产 -> 农田项目库的公文信息的页面切图与接口联调；土地承包的土地承包经验管理的切图与接口联调；重绘四大模块智分析的页面

4.农业农村 - 服务-人才培训 -> 培训计划页面的切图与接口联调

5.农业农村-生命周期系统 - >  首页页面的切图与接口联调，看板统计的切图与接口联调



11-12

完成：

1.农口直报系统检查文档 中的任务 十多处

2.农业农村->  首页 的 设计稿更新的页面切图交互与接口联调

3.省厅驾驶舱-> 进入页、建设用地、矿业发展、地址灾害、税费等页面以及各类详情页切图与接口联调、后续bug跟进修复与更新设计稿页面

进行中：

农口直报系统检查文档 剩余退回bug

省厅驾驶舱的后续bug跟进修复与更新设计稿页面







12-1

已完成：

1.省厅-> 驾驶舱	根据0105移动端指标核对文档等 修复文档提成的bug与更新页面

2.湖北全周期 - > 动作台 -> 业务统计的“查”与“登”两个页面的切图

初始化各个列表页面进行切图，土地储备的储备项目、储备地块专项债券三个页面对接口

首页工作台土地储备页面的更改

3.湖北土地二级市场交易 -> 首页 ：工作页面切图，中介服务模块->五个页面切图，交易服务模块->竞买资质、合同网签列表以及详情页等五个页面切图

4.农业农村首页设计稿更新、e-charts的bug修复，对外大屏修复图标bug

5.信用农安 台账bug，信用农安resgisterList日期默认bug

进行中：湖北土地二级市场交易各的接口联调







# 直报任务

## 多次点击搜索后，返回页面层级错误

第42个 可能是每次进搜索页面跳回来都是重新跳的页面



没有缓存，点过了回来直接跳台账去了，

然后自己加了back方法，现在只需要点两下了:laughing:

可里面明明有默认做了this.$router.back()

只有back回去才能缓存



把所有的back都换成replace，去积累一下





问代哥，

![image-20231124085747916](11%E6%9C%88.assets/image-20231124085747916.png)

这样页面间的传值，路由传参是正确方法吗

什么才是最正规的传值方法











## 组件缓存

不用组件缓存，用原生api，创一个新的容器





src\modules\InformationReported\views\village\components\List.vue



openNewPage
src/utils/webViewApi.ts







# 省厅任务



横竖屏样式切换



监听器监听resize 监听自己写的事件

 this.screenOrientation = screenWidth > screenHeight ? 'landscape' : 'portrait';  



用:class去切换样式







详情页的拖拽实现，拖拽部分用的position: absolute; 然后去控制样式top去挪位置







# SpringBoot

表现层	 ->					 业务层				->  			数据层

controller(restful)         service（加个实现层转调数据层）			dao（操作mapper，持久层）（extends BaseMapper<Book>）





# 一月TODO



双重router-view的情况下，keep-alive失效的问题



看到网上提到的是keep-alive没有提供清理指定缓存方法，只是给你设置黑白名单，所以封装了清除组件缓存的方法就是先移除再添加回来,这个行为称它为重置吧

```js
async function removeCacheEntry (componentName: string) {    
    if (removeCache(componentName)) {
      await nextTick()
      addCache(componentName)
    }
  }
```



include是以组件名为关键字控制缓存的方案，比如同个页面组件可能存在多份参数不同的缓存。本文是以页面对象为单位控制缓存





和



keep-alive的缓存对象是之下的直接组件





这里荣哥提到dom操作，我可以延伸到性能优化上





全屏后宽高变成零

mounted改为actived？

resize的事件不用箭头函数运行，Why

![image-20240123091948102](11%E6%9C%88.assets/image-20240123091948102.png)

好像不是因为箭头函数的this指向问题吧？因为resize还是有，只是在切换全屏时失效，而且里面也没用this呀







湖北全周期

面包屑导航实现，缓存实现					面包屑导航栏可以和contenMenu一起讲



给导航添加鼠标右键事件

```js
  @contextmenu.prevent="openMenu(tag, $event)"
```

![image-20240123093944049](11%E6%9C%88.assets/image-20240123093944049.png)



















为什么湖北全周期的页面更新后（回流后）会白屏











自定义指令完成一些功能

首页的那个节流等，看看里面的工具类







动态配置路由的实现



# Todo

面包屑路由的实现



pdf浏览实现细节



用v-show改造展示组件结构兼容iframe嵌入和独立文件预览缓存（keep-alive）细节（部门文档）



小农水沟渠





app个人中心点击头像，先进原生的，点击头像后修改头像后，再把结果给h5





[Open Source | Awesome Vue.js (awesome-vue.js.org)](https://awesome-vue.js.org/projects-using-vue-js/open-source.html)



[NGINXConfig | DigitalOcean](https://www.digitalocean.com/community/tools/nginx?domains.0.php.php=false&domains.0.routing.index=index.html&domains.0.routing.fallbackHtml=true&global.app.lang=zhCN)





12月底



地质灾害 柱状图 二合一



饼图外环去掉





问一下我这个职位未来进来之后可能会面临到的比较大的挑战是什么呢





滚动条添加了flex布局解决的







在 xx 情景（situation）下，我的任务（task）是，为此付出的行动（action），取得了 xx 结果（result）









把打印和跨页面一起讲





将图片转成base64的目的其实就减少http请求





深度学习图片预处理、归一化





# 手搓Grid

自己手搓动态列表完成需求，封装el-table、el-pagination，二次封装el-table-column，

场景：因新项目需求，后端列表需求需要精简、接口形式有所变化，需要维护之前的表单显示的基本组件

后端返回类型，前端在外写扁头类型需求，组件根据各种类型做处理，如：返回sort字段，前端按照sort排序；返回时间字段我就处理时间格式

编写工具类：处理时间（用插槽），处理表头格式，搜索词高亮





对于分页就是外面的size-chagne、chagne-page

![image-20240126165124869](11%E6%9C%88.assets/image-20240126165124869.png)







组件上写tag标签



slot的传值



统一在外配置header



列里面的宽高能自定义吗		可以 ：表头中写width属性对应el-table-column的width  ---- col.width

sortable属性 （布尔值）			

结合 :sort-orders="col.sortOrders || ['ascending', 'descending', null]"		点击表头的排序顺序



递归组件	htmlContent...

DynamicColumn自递归

判断自我调用的条件，否则会导致程序崩溃，相当于递归也需要一个结束递归的判断。

还有个动态组件  再就是render、异步组件等



讲讲

 v-bind="$attrs"

​    v-on="$listeners"

等所有功能实现后把这个去掉看看：就是接收了total等，$attrs里有全部没有用prop接收的属性

total

 :page-size.sync="searchParams.pageSize"

​    :current-page.sync="searchParams.pageIndex"





序号的处理

el-table-column的type="index"

showIndex这是默认开启的，

```vue
         <template slot-scope="scope">
            <span v-if="needPagination">
              {{ (($attrs['current-page'] || 1) - 1) * $attrs['page-size'] + scope.$index + 1 }}
            </span>
          </template>
```





一些外面传入功能的实现